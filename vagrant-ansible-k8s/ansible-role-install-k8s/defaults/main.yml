---
# defaults file for ansible-galaxy-k8s
docker_template_config_path: "{{ role_path }}/templates/docker"
core_mode: "{{ role_path }}/templates/mode"
core_config: "{{ role_path }}/templates/core_config"
